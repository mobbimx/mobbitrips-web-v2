import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Contacto',
  description: 'Contáctanos para reservas, dudas o información sobre nuestras propiedades.',
};

export default function ContactoPage() {
  return (
    <main id="main-content">
      <h1>Contacto — Mobbitrips</h1>
    </main>
  );
}
