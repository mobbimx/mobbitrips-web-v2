import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Servicios',
  description: 'Descubre todos los servicios que ofrecemos en nuestras propiedades.',
};

export default function ServiciosPage() {
  return (
    <main id="main-content">
      <h1>Servicios — Mobbitrips</h1>
    </main>
  );
}
